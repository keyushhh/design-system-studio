Pill toggle switch - emerald track when on. The one intentionally rounded control in the system.

```jsx
<Switch label="Redirection on share" checked={on} onChange={fn} />
<Switch checked disabled />
```