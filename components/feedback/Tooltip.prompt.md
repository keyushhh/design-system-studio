Dark hover/focus tooltip that wraps its trigger.

```jsx
<Tooltip label="Copies the deck URL"><Button iconLeft="link">Share</Button></Tooltip>
<Tooltip label="Hidden from export" placement="right"><Icon name="eye-off" /></Tooltip>
```

`placement`: top (default) / bottom / left / right.