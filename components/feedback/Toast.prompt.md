Flat status toast with a tone keyline + icon. Compose your own stack/timer.

```jsx
<Toast tone="success" title="Deck generated" message="14 slides built." onClose={dismiss} />
<Toast tone="error" title="Import failed" message="Missing frontmatter: client." />
```

Tones: success, error, info, brand.